/**
 * MCP tools as plain transport-agnostic functions (02-mcp-contracts.md).
 * The server, the CLI agent, and the eval harness all bind the same registry;
 * wire names use underscores (`analytics_balances`) — never dots (ADR-012).
 * Every response carries the citation envelope (invariants C1–C6).
 */
export {};

{
  "name": "@reconcil/recon",
  "version": "0.0.0",
  "private": true,
  "description": "Source-agnostic matching engine, match lifecycle, status derivation (HITL).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@reconcil/core": "workspace:*",
    "@reconcil/db": "workspace:*",
    "csv-parse": "^7.0.2"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "fast-check": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

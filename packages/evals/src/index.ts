/**
 * Eval harness (04-testing.md): fixture loaders, the FixtureProvider replay,
 * dataset parsing (core-30.yaml), deterministic graders G1–G5. No network in
 * any test; the Anthropic key is used only by eval runs, never by the core.
 */
export {};

{
  "name": "@pet-crypto/evals",
  "version": "0.0.0",
  "private": true,
  "description": "Golden fixtures (wallets, recorded provider responses), eval datasets, deterministic graders.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint src test",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@pet-crypto/core": "workspace:*",
    "@pet-crypto/db": "workspace:*",
    "@pet-crypto/ingestion": "workspace:*",
    "@pet-crypto/ledger": "workspace:*",
    "@pet-crypto/mcp-tools": "workspace:*",
    "yaml": "^2.6.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^11.0.0",
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "eslint": "catalog:",
    "pg": "^8.22.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

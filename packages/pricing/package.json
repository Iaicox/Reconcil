{
  "name": "@reconcil/pricing",
  "version": "0.0.0",
  "private": true,
  "description": "DefiLlama/CoinGecko/ECB adapters, daily snapshot service, valuation with pinned snapshot IDs.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint src test scripts",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "capture": "tsx scripts/capture-prices.ts"
  },
  "dependencies": {
    "@reconcil/core": "workspace:*",
    "@reconcil/db": "workspace:*",
    "decimal.js": "catalog:",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "catalog:",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "eslint": "catalog:",
    "fast-check": "catalog:",
    "pg": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}

/**
 * Exporters: monthly close pack, PDF summary, QBO/Xero journal CSV drafts,
 * audit manifests. Every journal artifact is labeled as a draft (P8);
 * rounding happens only here, at the export boundary (ADR-004).
 */
export {};
